How to run the Android App Manager:
	
	Option 1:  Double click on the AndroidAppManager jar file.  If Windows does not open this automatically, set it to open with Java.

	Option 2:  Navigate to this folder via the command line and then run this command:  java -jar AndroidAppManager.jar
